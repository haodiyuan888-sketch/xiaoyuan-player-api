name: Keep API Alive
on:
  schedule:
    - cron: '*/10 * * * *'  # 每 10 分钟唤醒一次
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Warm API
        run: curl -s -o /dev/null https://xiaoyuan-player-api.onrender.com/search/default
